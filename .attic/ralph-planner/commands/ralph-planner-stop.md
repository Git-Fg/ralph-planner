---
description: Emergency halt for Ralph Planner loop with graceful cleanup
argument-hint: (no arguments required)
allowed-tools:
  - Bash
  - Skill(ralph-planner)
---

# Ralph Planner Stop

Invoke the ralph-planner skill to halt the current loop and perform cleanup.

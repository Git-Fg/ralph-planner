---
description: Unified Ralph Planner with continuous execution (planning + execution in one loop)
argument-hint: Project goal + constraints (quoted if long) --max-iterations N --completion-promise "TEXT"
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Skill(ralph-planner)
---

# Ralph Planner Start

Invoke the ralph-planner skill with arguments: $ARGUMENTS

The ralph-planner skill handles all planning and execution logic in a continuous loop.
